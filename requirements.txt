# Direct dependencies, pinned to the exact versions this project is
# tested against (see requirements-lock.txt for the full, reproducible
# transitive closure — that's what production installs from).
#
# To upgrade a package: bump its pin here, then regenerate the lock file:
#   pip install -r requirements.txt
#   pip freeze --exclude-editable > requirements-lock.txt
# ...and re-run the test suite + `pip-audit` before committing either file.
Django==6.0.7
djangorestframework==3.17.1
django-cors-headers==4.9.0
celery==5.6.3
redis==8.0.1
django-celery-beat==2.9.0
pandas==3.0.5
numpy==2.5.1
scikit-learn==1.9.0
xgboost==3.3.0  # forward_return_rf classifier — walk-forward comparison vs RandomForest showed a consistent (if modest) skill improvement on both DSE/CSE; see market/services/ml_model.py
requests==2.34.2
beautifulsoup4==4.15.0
lxml==6.1.1
python-dotenv==1.2.2
httpx==0.28.1
tenacity==9.1.4
bdshare==1.2.3
openpyxl==3.1.5
certifi==2026.7.22
truststore==0.10.4  # verifies against the OS-native trust store — fixes real-world cert chains certifi's static bundle can't verify alone (see config/settings/base.py)
joblib==1.5.3
gunicorn==23.0.0
coverage==7.15.2
ruff==0.16.3

# --- Production infrastructure (Phase 8) --------------------------------
psycopg[binary]==3.3.4  # PostgreSQL driver — config/settings/production.py
whitenoise==6.12.0      # serves collectstatic output from the app process
